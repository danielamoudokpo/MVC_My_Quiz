{% extends 'base.html.twig' %}

{% block title %}Hello ScoreController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    {# {{ dump(Score)}} #}
    <h1>Your Final Score is {{ Score.grade }}! ✅</h1>
        {# {{ post.published_at|date("m/d/Y") }} #}
        <hr>
        <h3> You play The {{Score.Categorie.name}} Quiz </h3>
        <hr>
        <h3> You have answered {{Score.grade}}/10 Correct</h3>
        <hr>
        {% set var = 10 %}
        <h3> You have answered {{ var - Score.grade }}/10 Incorrect</h3>
        <hr>
        <h3>You palyed on the {{Score.createdAt|date("m/d/y")}}</h3>
</div>
{% endblock %}
